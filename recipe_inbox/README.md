# Recipe Inbox

Drop recipe files here to have the watcher build a printable brew packet.

Supported file types:
- `.md`
- `.markdown`
- `.txt` using the same markdown-style section format

Optional brew date:

```text
Brew Date: 2026-06-15
```

When the watcher is running, results appear in `brew_packets/<recipe-name>/index.html`.
