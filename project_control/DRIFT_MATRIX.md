# Drift Matrix

Purpose: keep repo truth, AI behavior, generated outputs, and operational files from drifting apart.

Use:
- Update only when a drift-sensitive area changes.
- Keep notes short.
- Do not track every file. Track areas where a change can silently break trust.

Status:
- Stable = reviewed and trusted
- Watch = changed or likely impacted, needs review
- Guarded = high-risk area, must pass checks before trust
- Stale = entry no longer reflects reality

| Area | Canonical Source | Depends On / Affects | Review Trigger | Required Check | Owner | Status | Last Reviewed | Notes |
|---|---|---|---|---|---|---|---|---|
| Core brewing context | `profiles/equipment.yaml`, `profiles/water_profiles.md`, `libraries/yeast_library.md` | brewing recommendations, process advice, troubleshooting answers | any change to profile structure, brewing defaults, or core brewing guidance | confirm these files still exist, still match prompt/index references, and fail-closed behavior is still appropriate | Human | Watch | 2026-03-22 | `equipment.yaml` modified — added `verified: true` + `verified_date`; prompt now gates recipe math on this field |
| Onboarding and session boot | `README.md`, `system_prompt.md`, `knowledge_index.md` | fresh-chat startup consistency, new-user behavior, repo entry path | README startup edit, onboarding flow change, startup instruction change | verify README startup path still matches prompt/index contract and does not promise behavior the repo no longer supports | Human + AI | Watch | 2026-03-28 | README now provides one canonical startup instruction and lists control-plane commands. |
| Knowledge retrieval map | `knowledge_index.md` | AI retrieval behavior, repo navigation, file selection | file move, file rename, folder add/remove, workflow change | verify every referenced path still exists and output locations still match repo layout | Human + AI | Stable | 2026-03-09 | `knowledge_index.md` remains the repo map; README AI workflow points users to it after `system_prompt.md`. |
| Prompt contract | `system_prompt.md` | all AI behavior, trust boundaries, workflow behavior | prompt edit, workflow change, new mode, renamed file, changed guardrail | compare prompt rules against actual repo structure and current working process; remove rules that no longer map to real files or scripts | Human | Watch | 2026-03-22 | `batch_logs/` retired; equipment verification gate added; artifact chain manifest added at Format A start; brew day sheet naming rule uses undated/dated states. |
| Repo control workflow | `project_control/DRIFT_MATRIX.md`, `project_control/REVIEW_RULES.md`, `project_control/insight_register.json`, `tools/drift_review.py`, `tools/intake_insight.py`, `tools/insight_report.py`, `tools/prepare_brew.py`, `tools/batch_lifecycle.py`, `tools/register_brew.py`, `tools/register_package.py`, `tools/yield_report.py`, `tools/batch_state_summary.py`, `tools/brew_op.py`, `tools/render_recipe_html.py`, `tools/refresh_recipe_html.py`, `tools/validate_recipe_html_sync.py`, `tools/prompt_harness.py`, `tools/validate_hop_aa_sync.py`, `Makefile` | drift review, validator execution, prompt-governance workflow, trust restoration process | control-doc edit, validator change, Makefile workflow edit, new trust-check tooling, new brew-prep/post-brew/package orchestration, new durable insight intake/reporting flow | run `python3 tools/drift_review.py`; confirm changed control files map cleanly to matrix rows and expected checks | Human + AI | Watch | 2026-03-30 | `brew-op` now acts as the single operator entry point, `batch-state --with-next-actions` surfaces explicit next moves, trust-check refreshes changed recipe HTML before validating sync, and durable rules now have an explicit capture queue via `insight_register.json`. |
| Hop AA sync | `libraries/inventory/stock.json` + `project_control/ACTIVE_ARTIFACTS.json` | active recipes, active printable HTML, active BeerXML exports, inventory-aware brewing logic | hop lot update, AA edit, active recipe/export edit touching hop values, active artifact scope change | run `python3 tools/validate_hop_aa_sync.py`; do not mark trusted unless result is `AA_SYNC_OK` | Human + AI | Guarded | 2026-03-28 | Validator scope is now active-artifact-only; historical/as-brewed files are excluded from current lot enforcement. |
| Inventory truth | `libraries/inventory/stock.json` | `tools/inventory_cli.py`, recipe feasibility, shopping logic, stock-aware suggestions | restock, brew completion, schema edit, inventory workflow edit | confirm stock schema still matches CLI expectations and brew/restock flows still make sense | Human + AI | Watch | YYYY-MM-DD | |
| Recipe lifecycle | `recipes/in_development/` and `recipes/locked/` | brew-day sheets, exports, repeatability | recipe promoted, recipe revised after batch, duplicate recipe created, BeerXML import/export change | confirm which recipe is current, which is locked, and that downstream artifacts reference the intended version | Human | Watch | 2026-03-22 | Iteration Delta Structure and Style Boundary Check added to prompt; `in_development/` contains files that may belong in `locked/` — needs placement review |
| Brew-day execution artifacts | recipe markdown + `libraries/inventory/stock.json` + `libraries/yeast_library.md` + `profiles/equipment.yaml` | `brewing/brew_day_sheets/`, brew execution decisions | new brew-day sheet, OG/volume change, yeast availability change, schedule/date change | confirm yeast source, generation, starter method, and brew-date-anchored dates are explicit and inventory-backed | Human | Watch | 2026-03-22 | Brew day sheet is now the single canonical artifact (brew log concept retired); naming rule: undated = planning, dated = committed brew; `git mv` workflow on brew date set |
| BJCP study isolation | `libraries/bjcp_study/` files | study behavior vs normal brewing behavior | prompt edit, study command change, new study file, mode handling edits | verify BJCP mode is still explicit opt-in and does not leak into standard brewing responses | Human | Stable | YYYY-MM-DD | |
